rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All data (usage counters) is accessed exclusively via the Admin SDK
    // from Cloud Functions. Clients have no direct Firestore access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
